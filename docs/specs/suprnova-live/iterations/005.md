# Suprnova Live -- Iteration 005

Status: Scope contract
Agreed: 2026-08-30
Amended: 2026-09-01 -- rebased the standalone implementation baseline on the
final hardening commit after committing the pending bootstrap wait,
documentation wording, and staged capture notes.

## In

Iteration 005 performs the atomic move from the dedicated development
repository into the Suprnova workspace and implements the complete RenderCache
foundation assigned to this iteration by Iteration 004. The move includes the
engine, browser runtime, generated artifacts, fixtures, tests, benchmarks,
implementation documentation, normative specification set, and structural
checker. After the cutover commit, `suprnova/crates/suprnova-live/` is the sole
maintained product and specification authority; the standalone repository is
historical development evidence and receives no parallel product changes.

The imported engine remains an internal crate. Applications consume Live and
RenderCache only through documented `suprnova::live`, `suprnova::view`, route,
configuration, macro, CLI, and testing surfaces. The internal engine does not
depend on the public framework crate, and generated application code never
names `suprnova_live` or another internal package.

The committed standalone implementation baseline is commit `d9636d7`, followed
only by the specification commit that rebases this baseline. Uncommitted
standalone files, ignored dependencies, local references, optional handoff
archives, and captured future-iteration notes are not migration input. The
import preserves committed history under `crates/suprnova-live/` rather than
flattening the project into an untraceable copy.

### Atomic workspace cutover

- Import the committed Live history beneath `crates/suprnova-live/` in an
  isolated Suprnova worktree based on the released clean `main` baseline.
- Move the product tree, normative specifications, checker, browser package,
  fixtures, tests, benchmarks, and implementation documentation together. No
  maintained authoritative copy remains in the standalone repository.
- Reconcile the nested Cargo workspace with Suprnova's root workspace. The
  internal crate and supporting packages inherit the workspace edition, MSRV,
  license, and release version where appropriate while protocol, snapshot,
  fixture, manifest, and capability versions remain independently governed by
  their existing compatibility contracts.
- Preserve the standalone crate's dependency pins unless Suprnova already owns
  a compatible exact or workspace dependency. Any version convergence that can
  change behavior receives focused conformance evidence rather than a lockfile-
  only assumption.
- Keep the standalone `reference/`, local `node_modules/`, generated qualification
  evidence, and optional Fable ZIP outside the product import unless a tracked
  file is required by a reproducible test or normative provenance contract.
- Update repository-local guidance, gates, paths, package scripts, fixtures,
  archive assumptions, and documentation so every checked path resolves from
  the integrated location.

### Suprnova framework integration

- Add a documented `suprnova::live` facade that owns configuration, component
  registration, mount and action entry points, typed outcomes, testing helpers,
  provider selection, and the hidden `__private` expansion contract required by
  generated code.
- Add a documented `suprnova::view` facade over the existing host-neutral view
  engine. Askama remains the normative checked substrate, `TrustedHtml` remains
  the only checked unescaped Live value, and handlers and components do not
  expose the internal engine or Askama implementation as the stable public API.
- Move production procedural macro ownership into `suprnova-macros`. Macro
  expansions continue to name only `::suprnova::live` and its documented hidden
  contract. The development macro and facade fixtures may remain only where
  they still provide non-duplicated conformance value.
- Register real Suprnova document, Live action, fresh-render, upload control and
  data, upload reacquisition, SSE control and stream, and WebSocket paths through
  the existing router and response machinery. Reserved Live paths remain
  versioned and collision-checked; application-owned reacquisition remains
  outside the reserved namespace.
- Adapt Suprnova request facts, body limits, sessions, CSRF, same-origin policy,
  authentication, tenant and authorization context, transactions, validation,
  events, broadcasting, storage, configuration, telemetry, cancellation, and
  HTTP response intent to the engine's typed host ports. Framework integration
  cannot bypass an engine trust boundary or reimplement a second Live state
  machine.
- Serve the exact deterministic ESM and classic-script core plus optional
  Stimulus, upload, and asynchronous artifacts through Suprnova's asset
  facilities. Applications need no JavaScript build tool merely to use the
  shipped runtime, and application-supplied Stimulus remains optional.
- Extend the CLI with non-destructive Live scaffolding, checked-template and
  contract validation, safe inspection, and asset installation or publication
  behavior consistent with existing Suprnova command and template conventions.
- Add a dogfood application surface and manual coverage that prove ordinary SSR,
  Live without RenderCache, Live with each supported cache profile, uploads,
  polling, SSE or WebSocket augmentation, recovery, and production artifact
  delivery through real framework paths.

### Render representations, variance, and storage

- Implement route and route-group RenderCache policy with deterministic
  inheritance, explicit eligibility, concrete-response safety checks, bypass,
  stale behavior, storage selection, and observability. Live works unchanged
  when RenderCache is disabled, unavailable, or bypassed.
- Implement bounded versioned Complete and Composite entry codecs. Complete
  entries hold immutable final response bytes and replayable safe metadata;
  Composite entries hold an integrity-protected typed segment graph and stitch
  slots whose final bytes, headers, CSP data, content length, and validator are
  computed only after current-request assembly.
- Implement canonical bounded lookup identities from trusted route, normalized
  parameters and query, negotiated representation, application and view build,
  and declared variance. Raw secrets, arbitrary cookies, unstable process IDs,
  and unbounded attacker-controlled dimensions never become keys or diagnostics.
- Implement public shared, public shell with private stitched segments, private
  cached, and uncacheable classification with automatic safety downgrades from
  observed identity, session, authorization, tenant, feature, configuration,
  locale, and request context.
- Implement opaque revocable private key dimensions and bounded retention.
  Anonymous and authenticated output, tenants, principals, permission versions,
  sessions, and private application contexts cannot collide or leak through
  bodies, metadata, keys, logs, metrics, or inspection output.
- Implement typed deterministic server stitching with bounded acyclic segment
  ownership, current authorization, request-specific metadata, duplicate DOM or
  island identity detection, and explicit fail-document, safe-fallback, or omit
  slot outcomes. Private content is never injected by the browser to complete
  initial rendering.
- Implement immutable in-process L0 plus atomic file-backed and database-backed
  L1 stores. Add one conforming externally accelerated Redis-backed profile by
  reusing Suprnova's established Redis facilities behind the RenderStore,
  instance-ledger, and rebuild-coordinator contracts. Provider selection does
  not route RenderCache bytes through the generic application value cache.
- Implement strong or weak represented-byte validators, conditional 304
  handling, safe header replay, content-encoding separation, private defaults
  for shared caches, and explicit bounded `s-maxage` or supported purge policy.
  Composite structural validators are never replayed as final HTTP validators.

### Dependency generations and coherent rebuilding

- Wrap the entire opted-in cacheable handler and rendering pipeline in one
  request-scoped async-safe dependency collector. Framework-owned route,
  locale, feature, configuration, identity, session, ORM, model, relation, and
  query reads register typed observations automatically; explicit custom APIs
  cover external authorities.
- Implement versioned typed dependency identities and durable logically append-
  only generation truth for SQLite, PostgreSQL, and MySQL or MariaDB. The
  application database remains generation authority at every deployment tier;
  memory, files, Redis, hints, and cached bodies cannot prove currentness.
- Integrate create, update, delete, restore, relationship, bulk, and supported
  raw-write paths so generation advancement commits atomically with owned data
  transactions and disappears on rollback. Unknown or insufficiently observable
  reads and writes collapse to broader safe generations or bypass caching.
- Associate rebuild data and generations inside one declared consistent read
  view, close that view after all lazy and template reads complete, then perform
  a fresh current-authority reread before publication. A mismatch discards the
  complete candidate and enters bounded retry, stale, or bypass policy.
- Implement Embedded, Database-coordinated, and Externally accelerated
  deployment profiles over independent RenderStore, LiveInstanceLedger,
  RebuildCoordinator, and GenerationLedger contracts. Tier 0 is the behavioral
  reference and requires no daemon; Tier 1 uses the shared database; Tier 2 uses
  Redis as acceleration while the database remains generation authority.
- Implement bounded validation leases and optional hints. Hints may invalidate
  local proof early but never extend validity. Restart, hint loss, accelerator
  eviction, or partition cannot make stale content appear current.
- Implement in-process and distributed singleflight with expiry, cancellation,
  bounded waiters, database-time or provider-time leases, monotonic fencing, and
  atomic publication. The contract guarantees one accepted publication per key
  and coherence fence, not universally one computation during failure.
- Implement explicit fresh, stale-servable, stale-on-error, and dead intervals,
  honest HTTP age and warning metadata, bounded background rebuild, failure
  backoff, negative-cache separation, and privacy-sensitive stale prohibitions.
- Namespace entry and generation formats by application/view build and authority
  epoch so rolling deployments cannot overwrite or reuse incompatible data.
  Provide an emergency epoch advance and bounded cleanup of unreachable entries.

### Tooling, evidence, and prior-contract continuity

- Integrate the Live checker and gate into Suprnova's normal check and release
  workflows without creating a second authoritative CI path. Clippy findings
  are reviewed without blanket `-D warnings`.
- Add deterministic RenderCache unit, property, integration, database, race,
  multi-principal, multi-tenant, multi-node, corruption, provider-failure,
  conditional-request, and dogfood tests. Tier 0 supplies the common provider
  conformance suite; higher tiers add their topology and failure cases.
- Add the isolated `render_cache_budget.rs` counting-allocator harness and
  shared-byte instrumentation for the Complete L0 path, plus C64 and C64+4
  latency, allocation, copy, database-round-trip, and dependency-reread
  workloads under architecture performance budget v1.
- Preserve every Iteration 001 through 004 protocol, security, browser,
  accessibility, resource, and artifact gate. Moving files or changing package
  topology cannot silently regenerate fixtures, rewrite reviewed artifact
  history, weaken exact production-artifact evidence, or convert a missing
  qualification into a pass.
- Carry Iteration 004's outstanding pinned-environment qualification honestly:
  `U4/16`, `E100/1K`, and `R100` require qualified S1 and B1 evidence, and the
  historical-baseline issue in its final repository-integrity criterion requires
  an explicit developer-approved resolution. Integration may proceed while
  these remain release blockers, but Iteration 005 cannot claim a fully qualified
  release by hiding or relabeling them.

## Out

Out means sequenced build order, not removal from the agreed system.

- Specs 20 through 25 remain the official component-library iteration. This
  iteration may scaffold and dogfood unstyled semantic Live views but does not
  implement the Tailwind component catalog, theme tokens, visual catalog, or
  theming studio.
- The captured document work arbiter, multi-island refresh batching, and
  alternative browser enhancement substrate ideas remain unconfirmed material
  under `iterations/next/`. They do not enter the integrated runtime through the
  repository move.
- Alpine, Lit, HTMX, a Stimulus fork, or another browser substrate does not
  replace the currently specified optional Stimulus integration in this
  version. That is a next-version architecture decision, not an Iteration 005
  implementation shortcut.
- No SPA or Turbo-style navigation, Inertia mixing, virtual DOM, browser-rendered
  private shell, streamed trusted HTML, generic RPC protocol, or synthesized
  no-JavaScript action path is introduced.
- No application imports the internal engine crate, macro development crate,
  Askama parser, Redis client, SeaORM internals, or browser implementation
  modules merely to use Live or RenderCache.
- No file in `/home/shawn/workspace2/suprnova-magnetar` is modified. Magnetar
  remains a separate subsystem consumed only through existing Suprnova
  integration boundaries.
- The standalone `suprnova-live` repository is not deleted, rewritten, or used
  as a second maintained branch after cutover. Archiving or removing that
  historical checkout is a separate repository-administration decision.

## Definition of done

Iteration 005 is done only when all of the following are true:

1. The Suprnova integration branch starts from the clean released `main`
   baseline, and the committed standalone Live history through this contract is
   imported beneath `crates/suprnova-live/` with traceable ancestry. Protected
   uncommitted and ignored standalone files are absent from the import.
2. Product code, normative specifications, checker, browser source and built
   artifacts, fixtures, tests, benchmarks, implementation documentation, and
   gate logic have one maintained authority under `crates/suprnova-live/`.
   Repository instructions and every checked relative path identify that
   authority; the standalone repository is historical only.
3. Suprnova's root workspace builds the engine and every retained supporting
   crate under workspace edition, MSRV, license, and release policy without a
   nested-workspace conflict, dependency cycle, crates.io publication path, or
   application-facing internal crate dependency.
4. `suprnova::live`, `suprnova::view`, and their required crate-root re-exports
   are documented and usable from a generated application. Hidden macro support
   is limited to `suprnova::live::__private`; application code and generated
   examples name no internal package.
5. Production Live macros live in `suprnova-macros`, retain their standalone UI
   diagnostics and conformance fixtures, and expand successfully in both the
   framework integration tests and a freshly scaffolded application.
6. Real Suprnova routes render canonical Askama-backed documents, mount public
   seeds or identity-bound instances safely, and adapt every Live action,
   fresh-render, upload, SSE, and WebSocket request through current middleware,
   session, CSRF, origin, tenant, authorization, rate, transaction, cancellation,
   and telemetry boundaries.
7. Hostile framework-adapter tests prove malformed, oversized, stale,
   cross-session, cross-tenant, cross-origin, unauthorized, duplicate, and
   retired requests fail before protected engine or application work. Adapter
   errors preserve typed recovery and redaction instead of leaking internal
   state or returning partial protocol bytes.
8. Suprnova serves the exact deterministic core, Stimulus, upload, and
   asynchronous ESM/classic artifacts and typed manifest. CSP, integrity,
   capability selection, version compatibility, deduplication, and no-build-tool
   application use pass through the real framework asset path.
9. CLI dry-run, conflict, invalid-name, scaffold, check, inspect, and asset
   workflows are non-destructive and tested. A generated Live application
   compiles and its templates pass the integrated checker without depending on
   the internal engine crate.
10. RenderCache policy is deterministic at route and group scope, concrete
    response eligibility fails closed, Live remains feature-complete with the
    cache disabled, and handler or store bypass never corrupts an existing
    valid entry.
11. Complete and Composite entry codecs are bounded, versioned, integrity-
    protected, corruption-tested, and inspectable without private body decoding.
    Complete validators bind exact final bytes; Composite validators are
    structural only until successful current-request assembly.
12. Canonical lookup keys distinguish meaningful route, query, host, locale,
    media, encoding, tenant, principal, permission, feature, config, application,
    and view dimensions as declared. Equivalent inputs normalize identically;
    different authorized variants cannot collide; secrets never enter keys or
    diagnostics.
13. Automatic classification can only preserve or reduce sharing. Multi-
    principal, multi-session, multi-tenant, logout, permission-change, and
    anonymous-private tests prove public, stitched, private, and uncacheable
    output never crosses an authority boundary.
14. Composite stitching accepts only typed slots and checked rendered output,
    reauthorizes each request-specific island, bounds depth and count, rejects
    cycles and duplicate identities, generates request-specific security data at
    assembly, and computes final status, headers, length, CSP, and validator only
    after every selected slot outcome succeeds.
15. L0 returns shared immutable Complete bytes with no full-body copy and at
    most four measured heap allocations in the isolated C64 harness. File and
    database L1 publication is atomic and torn writes become misses. Provider
    delay, corruption, eviction, and failure cannot make unproved bytes servable.
16. Conditional GET and HEAD behavior returns correct 304 or body responses for
    current Complete and assembled Composite representations. `Cache-Control`,
    `Vary`, age, warning, encoding validators, seed deadlines, and external
    private or bounded-shared policy agree with the represented variant.
17. One request-scoped collector covers opted-in handler data work through final
    template and metadata completion. It is async-safe, nestable, cleaned up,
    does not leak into detached tasks, and records framework-owned reads without
    changing ordinary non-cacheable execution.
18. SQLite, PostgreSQL, and MySQL or MariaDB generation ledgers preserve
    versioned logically append-only committed change semantics, authority epochs,
    backup and migration behavior, bounded compaction, and current generation
    lookup. Provider acceleration cannot replace database authority.
19. ORM and supported raw writes advance the necessary record, relation, query-
    class, model, or table generations in the owning transaction. Rollback
    advances none; deletes and empty-result dependencies retain broader authority;
    uncertain reads and writes collapse or bypass safely.
20. Deterministic race tests force writes before, during, and after the
    consistent render view, after that view closes, during the fresh authority
    reread, and before publication. No candidate with moved observations is
    stored or served as current.
21. Tier 0 works with one process, memory L0 and instance ledger, local rebuild
    coordination, file L1, and SQLite or the application's existing database,
    with no external daemon. It passes every common Live and RenderCache
    semantic conformance case and is not labeled degraded.
22. Tier 1 uses only the shared application database for instance CAS, fenced
    rebuild leases, generation truth, and optional L1 blobs. Multi-node tests
    cover rollback, leader death, lease expiry, fencing, clock policy, eviction,
    restart, and bounded duplicate computation.
23. Tier 2 uses Redis only as a conforming accelerator for bytes, instance
    records, rebuild coordination, and optional invalidation hints. Redis loss,
    partition, eviction, restart, delayed hints, and reconnect cannot prove stale
    generations current or weaken accepted-outcome and publication fencing.
24. Validation leases use bounded monotonic age, restart cold state, and
    authority rereads on expiry. Hints only shorten leases. Private and security-
    sensitive policies can require immediate authority checks.
25. Singleflight admits one accepted atomic publication per key and coherence
    fence, bounds waiters and duplicate work, releases resources on cancellation
    or failure, and prevents an expired or partitioned former leader from
    overwriting a newer result.
26. Fresh, stale-servable, stale-on-error, and dead behavior is policy-driven,
    bounded, private-safe, and observable. Known dependency mismatch remains
    visible; failed rebuild cannot publish partial output or extend staleness
    indefinitely.
27. Rolling build and entry-version tests prove incompatible nodes neither read
    nor overwrite one another's representations. Authority epoch advance
    invalidates safely, and old unreachable entries can expire without a
    correctness-dependent full scan.
28. The integrated test harness asserts route, ORM, template, serialization,
    and body-copy bypass on proven hits; Complete and Composite correctness;
    privacy and variance; generation transactions; race boundaries; provider
    conformance; stale policy; singleflight; and safe diagnostics without sleeps.
29. C64, C64+4, generation-reread, invalidation-storm, and multi-node workloads
    run under the architecture budget's exact environments and sample rules.
    Allocation, copy, latency, database-round-trip, fan-in, memory, and tail
    limits pass without disabling correctness, security, privacy, telemetry, or
    failure-path assertions.
30. Every unaffected Iteration 001 through 004 Rust, macro UI, fuzz, fixture,
    browser, CSP, accessibility, lifecycle, protocol, provider, artifact, and
    resource check still passes from the integrated paths. The ordinary Suprnova
    gate invokes the integrated Live gate without a second source of truth.
31. Iteration 004's unqualified S1/B1 workloads and historical-baseline issue
    remain visible release blockers until qualified evidence or an explicit
    developer-approved normative resolution exists. No renamed file, moved
    baseline, skipped command, local measurement, or gate condition converts
    missing evidence into success.
32. The manual documents component and view authoring, route registration,
    runtime assets, deployment profiles, cache policy, Complete and Composite
    behavior, privacy, generations, transactions, stale policy, observability,
    testing, operations, and recovery. The dogfood app exercises those public
    instructions through real Suprnova APIs.
33. `node crates/suprnova-live/scripts/check-specs.mjs`, the integrated browser
    checks, affected Rust checks, generated-application checks, Suprnova's normal
    gate, and the required release qualification all report their exact result.
    No placeholder, duplicate authority, stale standalone path, undocumented
    exception, blanket `-D warnings`, or knowingly deficient agreed branch
    remains when the iteration is called complete.
