commitment: upload-store-write-completion
commit: e8cacd75
examined:
  - `/usr/bin/git log --oneline 24e0a408..e8cacd75` and `git diff --stat` for the range: 14 commits, 193 changed tracked paths outside `.cairn/evidence/`.
  - `crates/suprnova-live/crates/suprnova-live-test-support/src/file_quarantine_store.rs` in full, all five `QuarantineStore` methods plus `write_all_fragmented` and `spawn_operation`, and the one-line diff that added the flush (`12a7df56`).
  - `.cairn/mechanisms/live-upload-store-flush` line by line, including the shell quoting, the node artifact-extraction filter and the `set -e` behaviour of the command substitution, without executing it.
  - `.cairn/evidence/runs/20260918T214602588Z-154556` and its `.out`: sixty `run N: ok` lines, exit 0, recorded against commit 782f5981.
  - `docs/commitments/upload-store-write-completion.md`, `docs/spec/live.md` (LIVE-038), `docs/spec/roadmap.md` item 15, the decision doc, and the backlog file's `Promoted to:` line.
  - `.cairn/escalations/live-010.md`, `loop-035.md`, `loop-035-2.md`, `loop-035-3.md`, and the recorded scope path lists in each.
  - `crates/suprnova-live/tests/upload_file_provider.rs`: the 27 test functions, the absence of any `#[ignore]` or `std::env::var` use, and the checksum and byte-count assertions in `chunks_stream_with_exact_duplicate_replay_and_whole_integrity` and `short_store_reads_and_writes_are_completed_without_whole_file_buffering`.
  - `/usr/bin/git show 8e785b6d` for the probe lockfile, then cross-checked every added package's version and checksum against the root `Cargo.lock` and confirmed `arc-swap`, `serde`, `serde_json`, `core_maths`, `allocator-api2` and `equivalent` were already present in the probe lock.
  - A grep for `tokio::fs`, `AsyncWriteExt`, `write_all` and `.write(` over `framework/src`, `crates/*/src`, `crates/suprnova-live/crates/*/src` and `suprnova-cli/src`, then read `framework/src/live/ports/upload_provider.rs` `write_at`/`sync`, `framework/src/http/upload/mod.rs` `collect_part`, `framework/src/live/tooling.rs`, `framework/src/render_cache/file_store.rs` and `suprnova-cli/src/secure_fs.rs`.
  - `framework/Cargo.toml` and `crates/suprnova-live/Cargo.toml` to place `suprnova-live-test-support`, plus its own `Cargo.toml` (`publish = false`).
  - The changelog restructure `45d9557f`: the 2.0.2 section at `e8cacd75` against the same section at tag `v2.0.2` for all seven files, the `LC_ALL=C` sorted body-line multiset before and after the commit for all seven files, the 2.1.0 entry counts, the section ordering, and the `.manual-translations.lock` CHANGELOG row against the seven new blob hashes in the diff headers.
  - `/usr/bin/git ls-remote origin refs/heads/main 'refs/tags/v2.1.0^{}'`, `git rev-parse v2.1.0^{commit}`, `git merge-base --is-ancestor`, and `git diff --stat fedc9089 250a6423`.
  - A scan of all 193 changed tracked files for U+2014 and U+2013.
findings:
  - resolved: LIVE-038 is satisfied by the change; `write_all_fragmented` now ends in `file.flush().await.map_err(provider_error)`, tokio's `poll_flush` drives the pending `State::Busy` blocking write to completion and surfaces a stored `last_write_err`, and because `read_at` and `sync` each open their own handle the flushed bytes are visible to them through the page cache, which is exactly what the requirement asks for (visibility to a following read, not durability).
  - resolved: no completion path is left buffered; tokio's `poll_write` drains the previous in-flight operation before it copies the next fragment, so an early `Err` return from inside the loop leaves nothing in flight, and the `written == 0` guard is unreachable for a tokio `File` because it buffers rather than short-writing, so every way out of the function either flushed or never had an outstanding write.
  - resolved: the flush cannot disturb the assertions the file already makes; it sits after the loop and never goes through `poll_write`, so `maximum_observed_write` stays bounded by the fragment limit that `short_store_reads_and_writes_are_completed_without_whole_file_buffering` asserts (`<= 2`).
  - resolved: the mechanism implements its falsifier faithfully; `seq 1 60` gives sixty runs of the whole test binary, each labelled `run N: ok`, and the first non-zero exit prints the failing run's tail and `exit 1`s, so a red run cannot be reported as a pass. libtest exits 101 on a failed assertion, the checksum and byte-count checks are real assertions reached through `.expect(...)`, there is no `#[ignore]` in the file, and the test binary reads no cargo-set environment variable, so running it directly is equivalent to `cargo test`. A missing `node` or a changed `--message-format json` shape yields an empty `$binary`, which fails run 1 rather than passing silently.
  - resolved: the fix cannot change shipped behaviour; `suprnova-live-test-support` is `publish = false` and appears only under `[dev-dependencies]` in `framework/Cargo.toml` and `crates/suprnova-live/Cargo.toml`, and the only non-test tokio-file writers, `framework/src/live/ports/upload_provider.rs` `write_at` and `framework/src/http/upload/mod.rs` `collect_part`, both already flush before they hand the object on (the provider's flush landed in `e63a68d5`, an ancestor of this range).
  - open: the recorded review's third finding says "a search over `framework/src`, `crates/*/src` and `suprnova-cli/src` finds only reads", and that is not true; both `framework/src/live/ports/upload_provider.rs` and `framework/src/http/upload/mod.rs` write through tokio files. The conclusion the sentence supports is still correct, because both of those writers flush, but the evidence as written is wrong and would send the next reader looking for this defect class down a false trail.
  - resolved: `framework/tests/fixtures/testing-off-probe/Cargo.lock` at `8e785b6d` is correct and minimal at 50 insertions and 1 deletion; it adds `charts-rs` 1.0.0, `fontdue` 0.9.4, `ttf-parser` 0.25.1, `hashbrown` 0.15.5 and `foldhash` 0.1.5 plus the `charts-rs` line in `suprnova-live`'s dependency list, and the single deletion is the `foldhash` to `foldhash 0.2.0` disambiguation that a second `foldhash` version forces. No existing package changes version, and every added version and checksum matches the root `Cargo.lock`.
  - resolved: the changelog restructure is text-preserving and lands the sections where they belong; the `LC_ALL=C` sorted body-line multiset of `45d9557f` is byte-identical before and after in all seven files, each 2.1.0 section holds exactly 30 entries, ordering reads 2.1.0 / 2.0.2 / 2.0.1 everywhere, the reconstructed 2.0.2 section is byte-identical to the one at tag `v2.0.2` in English, `de` and `pt-BR`, and the `es`, `fr`, `ja` and `zh-Hans` sections differ from the tag only by translation wording and full-width-parenthesis fixes that landed between `v2.0.2` and `24e0a408`, before this range.
  - resolved: the release commit the escalation approved is not the one that landed, and the substitution is sound; `loop-035-2` names `fedc9089`, which is not an ancestor of `e8cacd75`, while `250a6423` is, and the two trees differ only by the probe lockfile fix and the escalation record itself. `250a6423`'s own diff covers 74 paths rather than the 13 the escalation records, but the extra paths are the manual chapters, the three other lockfiles, `framework/Cargo.toml` and `.manual-translations.lock` that the escalation prose names and that mechanisms such as `ui-tokens` and `live-gate` already declare as inputs. Tag `v2.1.0` on origin resolves to `250a6423`, matching `origin/main`.
  - open: the tree under review is local only; `origin/main` is `250a6423`, so `782f5981` (the `loop-035-3` answer that authorises the retained lockfile fix) and `e8cacd75` (the recorded review) are unpushed, and anyone reading the repository from origin sees v2.1.0 without the answer or the review that cover it.
