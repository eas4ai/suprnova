DECISION

Question:   session-request-serialization is Done and the roadmap has no further item; which backlog item becomes the next commitment?
Recommend:  Promote a-template-s-live-key-never-reaches-the-runtime-which-reads-data-suprnova-live-key: the runtime accepts live:key for morph identity (or the render pipeline emits data-suprnova-live-key from it), with a checker fixture and a browser case proving a keyed control from a template, and the components drop their doubled attributes.
Because:    A keyed morph control written exactly as the checker requires has no effect at runtime, so every user who follows the manual gets the defect; the two remaining items are a one-attribute template fix (the form gallery's missing .prevent, which can ride along) and a test that hangs only under machine load.
If wrong:   The next commitment spends its cycle on a vocabulary unification while the upload provider's cancellation hang stays a gate-time hazard; the two closed records (the SSE issuance 403, closed by live-issuance-credentials, and the site's v2.0.1 manual, resolved by the 2.0.2 site update) were retired in this commit.
Instead:    Promote the upload_file_provider cancellation hang as a reliability commitment, or the form gallery's .prevent as a one-case commitment, or name a new item for the specification.

Reply: ok | instead | ask. If this isn't clear, ask me to explain it another way before you decide.

Concerns: OVL-006
Raised: 2026-09-16T02:26:22.800Z
Raised after: OVL-006=10
