# Browser conformance certificates

The Iteration 003 host currently exercises nonce and hash CSP over loopback HTTP. If a later
conformance case needs a secure-context-only transport feature, place only generated local-test
certificates here. Production certificates and keys must never enter this apparatus or the
repository.
