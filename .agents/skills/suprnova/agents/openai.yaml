interface:
  display_name: "Suprnova"
  short_description: "Repo-specific patterns and workflows for suprnova"
  default_prompt: "Use the suprnova repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true