### Application messages - English.
### Add locales by creating lang/<locale>/ with the same ids.

welcome = Welcome to { $app }!

# Chrome for the Error page (frontend/src/pages/Error.*), which the
# framework renders for every error response - see `.error_page("Error")`
# in src/bootstrap.rs. The `message` prop itself comes from the server.
error-reference = Reference:
error-go-home = Go home
